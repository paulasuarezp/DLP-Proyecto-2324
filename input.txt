class
	prog2;


	create
		inicio;

	//*feature potenciaAux(b:INTEGER, e:INTEGER): INTEGER is
		local
			total : INTEGER;
			aux : INTEGER;
		do
			from 
				aux := 0;
				total :=b;
			until aux > e loop
				total:= total * b;
				aux := aux + 1;
			end
			return total;
		end*//

	feature inicio is 
		local
			k : INTEGER;
		do
		k := 4;
		k := 2^3;
		println k;
		k := k^4;
		println k;
	end

end

run inicio(); 
