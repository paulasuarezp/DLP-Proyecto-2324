//*
Este ejemplo tiene varios errores semánticos que el compilador debe detectar en la
fase de identificación y otros que no deben ser detectados aún.
*//

class inicializadores_2_error;
	
	create
		inicio;

	feature inicio is
		do
	end

	feature otro is
		do
			inicio(3);	/* Inicio no necesita parámetros
	end

end

main
	otro();	/* Se intenta arrancar la aplicación con una feature no declarada como inicializador
end