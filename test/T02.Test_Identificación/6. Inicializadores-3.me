//*
Este ejemplo tiene varios errores semánticos que el compilador debe detectar en la
fase de identificación y otros que no deben ser detectados aún.
*//

class inicializadores_3_error;
	
	create
		inicio; /* se ha declarado el consructor, pero no se ha definido

	feature otro is
		do
	end

end

run	inicio(); /* se ha declarado el consructor, pero no se ha definido