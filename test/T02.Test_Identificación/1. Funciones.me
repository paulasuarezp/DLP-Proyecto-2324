//*
Este ejemplo tiene varios errores semánticos que el compilador debe detectar en la
fase de identificación y otros que no deben ser detectados aún.
*//

class funciones_error;
	
	create
		arranque;
	
	feature dobleSuma is
		do
	end
	
	feature prueba is
		do
	end

	feature dobleSuma is /* Función repetida 
		do
	end
	
	feature arranque is
		do
			print f();	/* Función f no definida 
			g(2);		/* Procedimiento g no definido 
			
			dobleSuma();
			dobleSuma(5.6);	/* No error en esta fase aunque no coincidan parámetros 
	end
end
	
main
	arranque();
end
