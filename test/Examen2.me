class examen2; /* Test con errores
    create main;

    feature greater(a: INTEGER): INTEGER is
        do
            return a > 1;
    end

    feature main is
        local
            a: INTEGER;
            d: DOUBLE;
        do
            a := 3;
            d := 3.3;

            greater(a=3);      /*  Error

            if 1 then           /*  Error
                a := d > 3.3;   /*  Error
            end

            a := 1 + (3 > 2);   /* Error

            print to<INTEGER>(a>3); /* Error

            until 1 loop            /* Error
                print -(3=2);       /*  Error
            end
    end

end

run main();
