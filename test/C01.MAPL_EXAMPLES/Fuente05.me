//*
Ejemplo Fuente05.
Divisor de un número
*//

class Fuente05;
	global
		vars
			numero, divisor : INTEGER;
			
	create
		main; 
			
	feature	main is
		/* local
			
		do
			read numero;
			from
				divisor := 1;
			until divisor > numero loop /* while (divisor <= numero)
				if (numero mod divisor = 0) then
					println divisor;
				end
				divisor := divisor + 1;
			end
	end /*main
	
end /* Fuente05

run main();