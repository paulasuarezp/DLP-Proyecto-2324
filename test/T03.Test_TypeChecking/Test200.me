class
	prog2;
	
global

	types
		deftuple point as /*estructura
			x: INTEGER;
			y: INTEGER;
		end
		deftuple complex as /*estructura
			i: DOUBLE;
			j: DOUBLE;
		end


	vars
		b : INTEGER;

	create
		inicio;

	feature factorial (i : INTEGER) : INTEGER is /* declaración con valor de retorno INTEGER y parámetro INTEGER
		local
			j, k : INTEGER;
		do
			from
				j:= 1;
				k:= 0;
			until i = 0 loop
				j := j * i;
				k := k + 1;
				i := i - 1;
			end
			return j; 
	end /* factorial

	feature inicioError is /* No es un constructor, no ha sido declarado en la cláusua 'create'
		local
			k, l : INTEGER;
			c : [3] INTEGER;
			p, q : point;

		do
			k := factorial (3);
			k := factorial (l);
			k := factorial (l > 1); /* No es error, los booleanos se manejan como INTEGER
			k := factorial (c[2]);
			k := factorial (p.x);
	end

	feature inicio is 
		local
			k, l : INTEGER;
			c : [3] INTEGER;
			p, q : point;

		do
			k := factorial (3);
			k := factorial (l);
			k := factorial (l > 1); /* No es error, los booleanos se manejan como INTEGER
			k := factorial (c[2]);
			k := factorial (p.x);
	end
end

run inicioError(); /* Error, 'inicioError' no ha sido declarada como constructor en la cláusula 'create'

//* ERRORES
Error en Type Checking: [67:5] La feature 'inicioError' llamada en 'run' no ha sido declarada como constructor en la cláusual 'create'.
*//